<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- One-scan Wi-Fi join (WifiNetworkSpecifier) for the combined QR. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- android:icon is @mipmap, not @drawable/app_icon: the launcher entry must
         be the ADAPTIVE icon (mipmap-anydpi-v26), or every launcher since
         Android 8 falls back to legacy handling and shrinks the tile onto a
         white circle instead of filling the mask. app_icon stays a drawable
         because the UI draws it directly. Both are generated — see
         backoffice/universal-platform/scripts/app-marks. -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- https App Links for the suite domain (verified via the domain's
                 assetlinks.json). The "/screens" prefix covers two QRs:
                   • /screens          — Step 1 "Get the app": installed → opens the
                     app; not installed → the browser shows the marketing page.
                   • /screens/connect  — Step 2 connection code: installed → opens
                     the app, which pairs from the URL's query/fragment (see
                     MainActivity.parseConnectPayload); not installed → the browser
                     shows the "scan this inside the app" page. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="opensource.unisim.co.uk"
                      android:pathPrefix="/screens" />
            </intent-filter>
            <!-- Custom scheme used by the combined connect payload. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="unisimscreens" />
            </intent-filter>
        </activity>

        <!-- Portrait QR scanner (zxing's default capture screen is landscape). -->
        <activity
            android:name=".PortraitCaptureActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme" />
    </application>
</manifest>
